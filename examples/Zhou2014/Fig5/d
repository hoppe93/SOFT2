
<Fig5base>

# Override settings from the baseline scenario
@ParticleGenerator PGen {
    thetap = 0.19, 0.19, 1; # Pitch angle (rad)
}

# Specify output file names
@RadiationOutput image   { output = "data/d-image.mat"; }
@RadiationOutput topview { output = "data/d-topview.mat"; }

