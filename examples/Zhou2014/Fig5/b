
<Fig5base>

# Override settings from the baseline scenario
@ParticleGenerator PGen {
    thetap = 0.02, 0.02, 1; # Pitch angle (rad)
}

# Specify output file names
@RadiationOutput image   { output = "data/b-image.mat"; }
@RadiationOutput topview { output = "data/b-topview.mat"; }

