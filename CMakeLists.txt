cmake_minimum_required(VERSION 3.9)
# Required features/version
# >= 3.9
#    + Simple linking/compilation with OpenMP

project(soft)
subdirs(src)

set(SOFT_VERSION_MAJOR 0)
set(SOFT_VERSION_MINOR 1)

